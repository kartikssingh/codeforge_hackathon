# ARIA — core runtime (~40 MB installed)
#
# This alone gives you a fully working shelter: typed intake, the deterministic
# protocol rule engine, the priority queue, escalation, inventory, dispatch,
# metrics and the whole dashboard.  No models, no GPU, no internet.
#
#   pip install -r backend/requirements.txt
#
# Add speech-to-text and RAG later with requirements-ml.txt — the app detects
# what is installed at runtime and degrades cleanly when something is missing.

fastapi==0.135.1
uvicorn==0.41.0
pydantic==2.12.5

# Optional but recommended (~3 MB): faster, more accurate fuzzy item matching.
# Without it ARIA falls back to difflib from the standard library.
RapidFuzz==3.14.3

# Deliberately NOT dependencies any more:
#   pandas       — replaced by the stdlib csv module (the ledger is ~20 rows)
#   APScheduler  — replaced by a daemon thread with deterministic shutdown
#   requests     — replaced by urllib for the handful of Ollama calls
